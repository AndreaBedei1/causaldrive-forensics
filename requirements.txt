# Runtime dependencies for carla-distributed-causal-forensics.
# The `carla` Python API is intentionally NOT listed here: it must match the
# simulator build and is installed from the CARLA distribution (see docs/ENVIRONMENT.md).
numpy>=1.20,<2.0
scipy>=1.7
networkx>=2.6
pandas>=1.3
PyYAML>=5.4
matplotlib>=3.4
pytest>=7.0
